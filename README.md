# Network_Sales
Онлайн платформа торговой сети электроники
## Описание
Веб-приложение с API-интерфейсом и админ-панелью.
## Задачи
1. Реализовать модель сети по продаже электроники.
    * Каждое звено сети ссылается только на одного поставщика.
    * Уровень иерархии определяется отношением к остальным элементам сети.
2. Каждое звено сети должно обладать следующими элементами:
    * Название.
    * Контакты:
        + email,
        + страна,
        + город,
        + улица,
        + номер дома.
    * Продукты:
        + название,
        + модель,
        + дата выхода продукта на рынок.
    * Поставщик (предыдущий по иерархии объект сети).
    * Задолженность перед поставщиком в денежном выражении с точностью до копеек.
    * Время создания (заполняется автоматически при создании).
3. Сделать вывод в админ-панели созданных объектов.
    На странице объекта сети добавить:
        * ссылку на «Поставщика»;
        * фильтр по названию города;
        * admin action, очищающий задолженность перед поставщиком у выбранных объектов.
4. Используя DRF, создать набор представлений:
    * CRUD для модели поставщика (запретить обновление через API поля «Задолженность перед поставщиком»).
    * Добавить возможность фильтрации объектов по определенной стране.
5. Настроить права доступа к API так, чтобы только активные сотрудники имели доступ к API.
## Зависимости
* `django`
* `djangorestframework`
* `djangorestframework-simplejwt`
* `django-filter`
* `django-cors-headers`
* `python-dotenv`
* `psycopg2-binary`
* `drf-spectacular`
## Линтеры
* `flake8`
* `black`
* `mypy`
* `isort`
### Эндпоинты
* Аутентификация пользователя.
* Список элментов сети. Имеется возможность фильтрации по поставщику, городу и стране.
    Реализована пагинация сортировка и посковый запрос.
* Эндпоинты CRUD элементов сети.
* Эндпоинты CRUD товаров.
## Локальная установка
1. Клонировать проект
    ```
    https://github.com/Ramazan-Z/Network_Sales
    ```
2. Записать чувствительные данные в файл `.env.example` и сохранить его как `.env.`
    Необходимые данные:
        * Секретный ключ django и флаг дебага.
        * Данные для подключения к postgresql.
        * Данные для регистрации суперпользователя.
        * Флаг необходимости заполнения базы данных иэ фикстур.
3. Сборка образа приложения и запуск с использованием docker
    ```
    docker compose up -d --build
    ```
4. Остановка
    ```
    docker compose down
    ```
### Документация
Описание осноных моделей и их полей:
```
http://localhost:8000/swagger/
```
